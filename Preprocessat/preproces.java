
//https://www.journaldev.com/2335/read-csv-file-java-scanner
//https://es.stackoverflow.com/questions/38085/leer-fichero-formato-csv-en-java
//http://javainutil.blogspot.com/2013/04/java-manejo-de-cadenas-metodo.html
//https://jarroba.com/map-en-java-con-ejemplos/
//https://www.it-swarm-es.com/es/java/como-poner-obtener-valores-en-desde-hashmap-anidado/1071335272/

package com.journaldev.csv;

import java.io.BufferedReader;
import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;
import java.io.Map;
import java.util.List;
import java.util.Arraylist;
import java.util.Scanner;


public class ReadCSV {
    /*String path;
    Integer numcol, numfil;
    List<List<String>> content;

    private static boolean separador(char c){
        return c == ";" or c == "," or "\t";
    }

    public static void main(Strings[] args) throws IOException{
        BufferedReader reader = new BufferedReader (new FileReader("ratings.db.csv"));
        String line = null;
        Scanner scanner = new Scanner(reader);
        int index = 0;
        //mejor hasmap
        //Map<int, Map<int, float>> rating = new TreeMap<int, Map<int, float>>();

        while (scanner.hasNextLine()){
            String data = scanner.nextLine();
            List<String> datasplit = Arrays.asList(data.split(",")); //devuelve lista de strings separados por el split
            content.add(datasplit);

            int userID, itemID;
            float rate;
            userID = Integer.parseInt(data);
            itemID = Integer.parseInt(data);
            rate = Float.parseFloat(data);

            index++;


            index = 0;
        }
        reader.close();

        //imprimir map
        while(it.hasNext()){
            int userID = it.next();
            int itemID = map.get(key);
            float rate = map.get(userID).get(itemID);
            System.out.println("UserID: " + userID + " ItemId: " + itemID + " Rating: "
                    + rate);
        }
    }*/
    public static void main(String[] args) {
        System.out.println("h");
    }
}
