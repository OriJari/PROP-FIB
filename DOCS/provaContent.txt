Prova 1:

Descripció: Cargar Datos, Content.

Objetivos: Realizar una correcta proceso de los datos para el nuevo formato de preprocesado.
Esta prueba no consta en dessarollo debido a que es una clase complementaria. Se ha usado este driver como pruebas para la interacción.

Driver: DriverContent.

Entrada: provaContent.txt, prueba del conjunto de dominio.controladores.clases.atribut.content para ver el funcionamiento.

Salida: estructura del tipo dominio.controladores.clases.atribut.content, permite analizar el comportamientio del preprocesado de datos.

Resultado: ok.

Opertativa:

Atencion:

Esta clase es complementaria por lo que es prescindible.

Se exponen una serie de elementos "content" para poder introdcuir y verificar la formación de la estructura.

En el caso de operaciones con parametros se realiza primero el numero del caso y
seguidamente el valor del parametro. Si se trata de una lista se pone el número y al
dar intro se siguen las intrucciones poniendo el número de elementos a añadir y junto a este dichos
elementos.
